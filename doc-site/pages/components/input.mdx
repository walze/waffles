import * as InputExports from '@datacamp/waffles/input';

import Layout from '../../components/content-layout';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/input/playground-config';
import BasicUsage from '../../examples/input/basic-usage';
import Sizes from '../../examples/input/sizes';
import Types from '../../examples/input/types';
import States from '../../examples/input/states';
import WithEnhancers from '../../examples/input/with-enhancers';
import Inverted from '../../examples/input/inverted';

export default Layout;

# Input

🚧 This page is **under construction**.

Use [Form Field](/components/form-field) utility component to enhance this component with: an accessible **label**, an optional **description**, an **error message** or to show **required indicator**.

## Accessibility

When using this component without `FormField` be mindful to provide `aria-label` or at least meaningful `placeholder` text.

## Examples

<Example title="Basic Usage" path="input/basic-usage">
  <BasicUsage />
</Example>

<Example title="Sizes" path="input/sizes">
  <Sizes />
</Example>

<Example title="Types" path="input/types">
  <Types />
</Example>

<Example title="States" path="input/states">
  <States />
</Example>

<Example title="With Enhancers" path="input/with-enhancers">
  <WithEnhancers />
</Example>

<Example title="Inverted" path="input/inverted" darkPreview>
  <Inverted />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### Input

In addition to all custom props listed below, `Input` accepts all props regular `HTMLInput` would, such as `type`, `placeholder`, `disabled`, `autocomplete`, `aria-` and `data-` attributes. Keep in mind that when `value` is specified `onChange` handler must be also provided.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/input/input-internal')}
/>

### Input.Enhancer

In addition to `children` prop listed below, `Input.Enhancer` accepts all props regular `HTMLButton` would, such as `onClick`, `aria-` and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/input/enhancer')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={InputExports} path="@datacamp/waffles/input" />
