import * as SwitchExports from '@datacamp/waffles/switch';

import Layout from '../../components/page-layout';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/switch/playground-config';
import BasicUsage from '../../examples/switch/basic-usage';
import States from '../../examples/switch/states';
import Inverted from '../../examples/switch/inverted';

export default Layout;

# Switch

🚧 This page is **under construction**.

## Examples

<Example title="Basic Usage" path="switch/basic-usage">
  <BasicUsage />
</Example>

<Example title="States" path="switch/states">
  <States />
</Example>

<Example title="Inverted" path="switch/inverted" darkPreview>
  <Inverted />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

In addition to all custom props listed below, `Switch` accepts all props regular `HTMLInput` would, such as `aria-` and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/switch/switch-internal')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={SwitchExports} path="@datacamp/waffles/switch" />
