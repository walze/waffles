import Layout from '../../components/content-layout';
import BestPractices from '../../components/best-practices';
import AllAssetTypes from '../../components/all-asset-types';
import Example from '../../components/example';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import BasicUsage from '../../examples/datacamp-branding/basic-usage';
import CustomSize from '../../examples/datacamp-branding/custom-size';
import InvertedMonochrome from '../../examples/datacamp-branding/inverted-monochrome';
import Inverted from '../../examples/datacamp-branding/inverted';
import Monochrome from '../../examples/datacamp-branding/monochrome';

export default Layout;

# DataCamp Branding

🚧 This page is **under construction**.

## Examples

<Example title="Basic Usage" path="datacamp-branding/basic-usage">
  <BasicUsage />
</Example>

<Example title="Custom Size" path="datacamp-branding/custom-size">
  <CustomSize />
</Example>

<Example title="Monochrome" path="datacamp-branding/monochrome">
  <Monochrome />
</Example>

<Example title="Inverted" path="datacamp-branding/inverted" darkPreview>
  <Inverted />
</Example>

<Example
  title="Inverted and Monochrome"
  path="datacamp-branding/inverted-monochrome"
  darkPreview
>
  <InvertedMonochrome />
</Example>

## Props

### DataCamp Branding

In addition to `inverted` and `monochrome` listed below, DataCamp Branding components accept all props a regular `SVGElement` would, such as `width`, `height`, `aria-` and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/datacamp-branding/datacamp-branding-internal')}
/>

## Imports

You can import any existing DataCamp branding component as a separate component. For example to import the `DataCampLogo` asset use:

<Imports module={['DataCampLogo']} path="@datacamp/waffles/datacamp-branding" />
