import * as SelectExports from '@datacamp/waffles/select';

import Layout from '../../components/content-layout';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/select/playground-config';
import BasicUsage from '../../examples/select/basic-usage';
import Sizes from '../../examples/select/sizes';
import States from '../../examples/select/states';
import WithPlaceholder from '../../examples/select/with-placeholder';
import Inverted from '../../examples/select/inverted';

<Layout title="Select" description="🚧 This page is under construction">

## Usage Guidance

Use [Form Field](/components/form-field) utility component to enhance this component with: an accessible **label**, an optional **desciription**, an **error message** or to show **required indicator**.

## Accessibility

When using this component without `FormField` be mindful to provide `aria-label` or at least meaningful placeholder option.

## Examples

<Example title="Basic Usage" path="select/basic-usage">
  <BasicUsage />
</Example>

<Example title="Sizes" path="select/sizes">
  <Sizes />
</Example>

<Example title="States" path="select/states">
  <States />
</Example>

<Example title="With Placeholder" path="select/with-placeholder">
  <WithPlaceholder />
</Example>

<Example title="Inverted" path="select/inverted" darkPreview>
  <Inverted />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### Select

In addition to all custom props listed below, `Select` accepts all props regular `HTMLSelect` would, such as `disabled`, `aria-` and `data-` attributes. Keep in mind that when `value` of currently selected option is specified `onChange` handler must be also provided.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/select/select-internal')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={SelectExports} path="@datacamp/waffles/select" />

</Layout>
