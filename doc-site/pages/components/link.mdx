import * as LinkExports from '@datacamp/waffles/link';
import Code from '@datacamp/waffles/code';
import Text from '@datacamp/waffles/text';

import Layout from '../../components/content-layout';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/link/playground-config';
import BasicUsage from '../../examples/link/basic-usage';
import Inverted from '../../examples/link/inverted';
import WithIcons from '../../examples/link/with-icons';

<Layout title="Link" description="Used for navigational actions that appear within or directly following a sentence">

## Usage Guidance

For standalone navigation actions use plain `Button` instead.

## Examples

<Example title="Basic Usage" path="link/basic-usage">
  <BasicUsage />
</Example>

<Example title="With Icons" path="link/with-icons">
  <WithIcons />
</Example>

<Example title="Inverted" path="link/inverted" darkPreview>
  <Inverted />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### Link

In addition to all custom props listed below, `Link` accepts all props regular `HTMLAnchor` would, such as: `href`, `aria-` and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/link/link-internal')}
  isPolymorphic
/>

## Imports

You can import following components or utilities from this module:

<Imports module={LinkExports} path="@datacamp/waffles/link" />

</Layout>
