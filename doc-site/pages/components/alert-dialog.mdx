import * as AlertDialogExports from '@datacamp/waffles/alert-dialog';

import Layout from '../../components/content-layout';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/alert-dialog/playground-config';
import BasicUsage from '../../examples/alert-dialog/basic-usage';

export default Layout;

# Alert Dialog

🚧 This page is **under construction**.

## Accessibility

The same accessibility guidelines apply to `Alert Dialog` as to [Dialog](/components/dialog/#accessibility).

## Examples

<Example title="Basic Usage" path="alert-dialog/basic-usage">
  <BasicUsage />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### AlertDialog

In addition to all custom props listed below, `AlertDialog` accepts all props regular `HTMLDiv` would, such as `aria-labelledby`, `aria-describedby`, `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/alert-dialog/alert-dialog-internal')}
/>

### AlertDialog.Header

In addition to all custom props listed below, `AlertDialog.Header` accepts all props regular `HTMLHeading` would, such as `id`, and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/alert-dialog/header')}
/>

### AlertDialog.Body

In addition to all custom props listed below, `AlertDialog.Body` accepts all props regular `HTMLDiv` would, such as `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/alert-dialog/body')}
/>

### AlertDialog.Footer

In addition to all custom props listed below, `AlertDialog.Footer` accepts all props regular `HTMLDiv` would, such as `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/alert-dialog/footer')}
/>

### AlertDialog.Button

In addition to all custom props listed below, `AlertDialog.Button` accepts all props Waffles [Button](/components/button/#props) would.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/dialog/button-internal')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={AlertDialogExports} path="@datacamp/waffles/alert-dialog" />
