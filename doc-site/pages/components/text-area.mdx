import * as TextAreaExports from '@datacamp/waffles/text-area';

import Layout from '../../components/content-layout';
import PageHeader from '../../components/page-header';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/text-area/playground-config';
import BasicUsage from '../../examples/text-area/basic-usage';
import States from '../../examples/text-area/states';
import AutoGrow from '../../examples/text-area/auto-grow';
import CharacterCount from '../../examples/text-area/character-count';
import Inverted from '../../examples/text-area/inverted';
import Sizes from '../../examples/text-area/sizes';

<Layout header={<PageHeader title='Text Area' underConstruction />}>

## Usage Guidance

Use [Form Field](/components/form-field) utility component to enhance this component with: an accessible **label**, an optional **desciription**, an **error message** or to show **required indicator**.

## Accessibility

When using this component without `FormField` be mindful to provide `aria-label` or at least meaningful `placeholder` text.

## Examples

<Example title="Basic Usage" path="text-area/basic-usage">
  <BasicUsage />
</Example>

<Example title="States" path="text-area/states">
  <States />
</Example>

<Example title="Auto Grow" path="text-area/auto-grow">
  <AutoGrow />
</Example>

<Example title="Character Count" path="text-area/character-count">
  <CharacterCount />
</Example>

<Example title="Inverted" path="text-area/inverted" darkPreview>
  <Inverted />
</Example>

<Example title="Sizes" path="text-area/sizes">
  <Sizes />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### TextArea

In addition to all custom props listed below, `TextArea` accepts all props regular `HTMLTextArea` would, such as `placeholder`, `disabled`, `rows`, `autocomplete`, `maxLength`, `aria-` and `data-` attributes. Keep in mind that when `value` is specified `onChange` handler must be also provided.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/text-area/text-area-internal')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={TextAreaExports} path="@datacamp/waffles/text-area" />

</Layout>
