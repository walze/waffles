import Layout from '../../components/content-layout';
import BestPractices from '../../components/best-practices';
import AssetGrid from '../../components/asset-grid';
import Example from '../../components/example';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import BasicUsage from '../../examples/asset/basic-usage';
import CustomSizeColor from '../../examples/asset/custom-size-color';
import { Notification } from '@datacamp/waffles/notification';
import { Link } from '@datacamp/waffles/link';

<Layout title="Asset" description="🚧 This page is under construction">

## Usage Guidance

Assets are **responsive** by default and will fit into their parent container if no `width` or `height` is provided (whilst still adhering to the `viewBox` aspect ratio).

They will also inherit the `color` of their container by default too.

<Notification
  title="DataCamp Brand"
  description={
    <>
      If you're looking for DataCamp Logos/Logomarks, see our{' '}
      <Link href="../brand">Brand</Link> components.
    </>
  }
/>

## Assignment Assets

<AssetGrid assetType="Assignment" />

## 3D Assets

<AssetGrid assetType="3d" />

## ALPA Loop Assets

<AssetGrid assetType="Alpa" maxColumns="2" hasDarkBackground />

## Logo Assets

<AssetGrid assetType="Logo" maxColumns="3" />

## Logomark Assets

<AssetGrid assetType="Logomark" />

## Other Assets

<AssetGrid assetType="Other" />

## Examples

<Example title="Basic Usage" path="asset/basic-usage">
  <BasicUsage />
</Example>

<Example title="Custom Size and Color" path="asset/custom-size-color">
  <CustomSizeColor />
</Example>

## Props

### Asset

Assets accept all props a regular `SVGElement` would, such as `width`, `height`, `aria-` and `data-` attributes.

## Imports

You can import any existing asset as a separate component. For example to import the `AvatarPlaceholderColored` asset use:

<Imports module={['AvatarPlaceholderColored']} path="@datacamp/waffles/asset" />

</Layout>
