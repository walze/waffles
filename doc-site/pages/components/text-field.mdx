import * as TextField from '@datacamp/waffles/text-field';

import Layout from '../../components/page-layout';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/text-field/playground-config';
import BasicUsage from '../../examples/text-field/basic-usage';
import Sizes from '../../examples/text-field/sizes';
import Types from '../../examples/text-field/types';
import States from '../../examples/text-field/states';
import Required from '../../examples/text-field/required';
import WithEnhancers from '../../examples/text-field/with-enhancers';
import Inverted from '../../examples/text-field/inverted';

export default Layout;

# Text Field

🚧 This page is **under construction**.

## Examples

<Example title="Basic Usage" path="text-field/basic-usage">
  <BasicUsage />
</Example>

<Example title="Sizes" path="text-field/sizes">
  <Sizes />
</Example>

<Example title="Types" path="text-field/types">
  <Types />
</Example>

<Example title="States" path="text-field/states">
  <States />
</Example>

<Example title="Required" path="text-field/required">
  <Required />
</Example>

<Example title="With Enhancers" path="text-field/with-enhancers">
  <WithEnhancers />
</Example>

<Example title="Inverted" path="text-field/inverted" darkPreview>
  <Inverted />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### TextField

In addition to all custom props listed below, `TextField` accepts all props regular `HTMLtext-field` would, such as `aria-` and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/text-field/text-field-internal')}
/>

### TextField.Enhancer

In addition to `children` prop listed below, `TextField.Enhancer` accepts all props regular `HTMLButton` would, such as `onClick`, `aria-` and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/input/enhancer')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={TextField} path="@datacamp/waffles/text-field" />
