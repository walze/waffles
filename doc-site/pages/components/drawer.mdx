import * as DrawerExports from '@datacamp/waffles/drawer';

import Layout from '../../components/content-layout';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/drawer/playground-config';
import BasicUsage from '../../examples/drawer/basic-usage';

<Layout title="Drawer" description="🚧 This page is under construction">

## Accessibility

The same accessibility guidelines apply to `Drawer` as to [Dialog](/components/dialog/#accessibility).

## Examples

<Example title="Basic Usage" path="drawer/basic-usage">
  <BasicUsage />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### Drawer

In addition to all custom props listed below, `Drawer` accepts all props regular `HTMLDiv` would, such as `aria-labelledby`, `aria-describedby`, `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/drawer/drawer-internal')}
/>

### Drawer.Header

In addition to all custom props listed below, `Drawer.Header` accepts all props regular `HTMLHeading` would, such as `id`, and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/dialog/header')}
/>

### Drawer.Body

In addition to all custom props listed below, `Drawer.Body` accepts all props regular `HTMLDiv` would, such as `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/dialog/body')}
/>

### Drawer.Footer

In addition to all custom props listed below, `Drawer.Footer` accepts all props regular `HTMLDiv` would, such as `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/dialog/footer')}
/>

### Drawer.Button

In addition to all custom props listed below, `Drawer.Button` accepts all props Waffles [Button](/components/button/#props) would.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/dialog/button-internal')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={DrawerExports} path="@datacamp/waffles/drawer" />

</Layout>
