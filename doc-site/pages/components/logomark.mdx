import Layout from '../../components/content-layout';
import BestPractices from '../../components/best-practices';
import Example from '../../components/example';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

export default Layout;

# Logomark

🚧 This page is **under construction**.

## Examples

## Props

### Logomark

Logomarks accept all props a regular `SVGElement` would, such as `width`, `height`, `aria-` and `data-` attributes.

## Imports

You can import any existing asset as a separate component. For example to import the `GitLogomark` asset use:

<Imports module={['GitLogomark']} path="@datacamp/waffles/logomark" />
