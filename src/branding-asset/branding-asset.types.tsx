export type BrandingAssetGenericProps = {
  /* Whether the branding asset is inverted in color */
  inverted?: boolean;
  /* Whether the branding asset is monochrome in color */
  monochrome?: boolean;
} & React.SVGAttributes<SVGElement>;
